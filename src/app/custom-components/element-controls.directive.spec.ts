import { ElementControlsDirective } from './element-controls.directive';

describe('ElementControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementControlsDirective();
    expect(directive).toBeTruthy();
  });
});
