import { TourGuideDirective } from './tour-guide.directive';

describe('TourGuideDirective', () => {
  it('should create an instance', () => {
    const directive = new TourGuideDirective();
    expect(directive).toBeTruthy();
  });
});
