import { InheritTestDirective } from './inherit-test.directive';

describe('InheritTestDirective', () => {
  it('should create an instance', () => {
    const directive = new InheritTestDirective();
    expect(directive).toBeTruthy();
  });
});
