import { InputClassesDirective } from './input-classes.directive';

describe('InputClassesDirective', () => {
  it('should create an instance', () => {
    const directive = new InputClassesDirective();
    expect(directive).toBeTruthy();
  });
});
