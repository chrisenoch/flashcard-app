import { InputHelperTextClassesDirective } from './input-helper-text-classes.directive';

describe('InputHelperTextClassesDirective', () => {
  it('should create an instance', () => {
    const directive = new InputHelperTextClassesDirective();
    expect(directive).toBeTruthy();
  });
});
