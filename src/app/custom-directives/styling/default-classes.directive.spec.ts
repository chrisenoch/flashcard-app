import { DefaultClassesDirective } from './default-classes.directive';

describe('DefaultClassesDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultClassesDirective();
    expect(directive).toBeTruthy();
  });
});
