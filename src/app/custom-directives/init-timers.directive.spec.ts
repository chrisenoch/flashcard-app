import { InitTimersDirective } from './init-timers.directive';

describe('InitTimersDirective', () => {
  it('should create an instance', () => {
    const directive = new InitTimersDirective();
    expect(directive).toBeTruthy();
  });
});
