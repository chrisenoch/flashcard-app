import { DisplayControlsDirective } from './display-controls.directive';

describe('DisplayControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayControlsDirective();
    expect(directive).toBeTruthy();
  });
});
